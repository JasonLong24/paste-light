#ifndef FORMAT_FILE_H
#define FORMAT_FILE_H

using namespace std;

extern void generate_html_view(const string& file);
extern void format_file(const string& file);
extern void generate_plain_text(vector<string> files);

#endif
